CREATE TABLE AGENDAMENTO (
	NUM_ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	TXT_NOME VARCHAR(100) NOT NULL,
	DAT_DATA DATETIME NOT NULL,
	TXT_TELEFONE VARCHAR(50) NOT NULL,
	TXT_EMAIL VARCHAR(50),
	TXT_OBSERVACAO VARCHAR(100),	
	IND_CONFIRMADO CHAR(1) NOT NULL DEFAULT 'N'
);

ALTER TABLE AGENDAMENTO ADD (IND_CONFIRMADO CHAR(1) NOT NULL DEFAULT 'N');

